from django.apps import AppConfig


class PreviewRunnerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.preview_runner"
    verbose_name = "Preview Runner"
